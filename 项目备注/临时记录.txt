
效率性优化：
1. 统一 Provider 实例，避免MyApp 与 main () 函数重复创建；2. 移除 main () 中缩略图生成逻辑，迁移至专属工具类或服务层；（已能运行）
2. Video 类添加 fromJson 和toJson 方法；2. Tag 类添加序列化 / 反序列化方法，并对其它代码中需要使用序列化或反序列化的地方做适配，有修改过代码的地方记得做dart format或者analytics确认文件无误；（已能运行）

1. VideoProvider 补充错误处理和加载状态管理；（已能运行）
2.TagProvider 优化标签计数更新逻辑，减少无效刷新；（已能运行）
3. VideoRepository 优化_updateTagCounts 方法，减少数据库访问次数；（已能运行）
4.TagRepository 添加批量操作功能，提升批量处理效率；（已能运行）



针对以下要求做修改：
index文件是指前端文件在build/web/index.html
压缩操作是指lib/services/video_compression_service



服务器页是指lib/ui/pages/server_control_page
提示框组件是指lib/utils/toast_utils
计时组件是指lib/widgets/timer_display


压缩操作是指lib/services/video_compression_service
服务器页是指lib/ui/pages/server_control_page
计时组件是指lib/widgets/timer_display


已处理：
等下开启服务器测试上传视频，是否有缩略图（已处理）
当前在处理的是握手问题，避免刷新后需要重新握手（已处理）
优化了服务器控制页面的布局，左右分区，文字清晰（已处理）
做完服务器控制页面布局后，可以制作导出json（已处理）
3、上传文件时候的tittle应该不包含后缀名；（已处理）
5、修复index的视频播放器，兼容电脑的谷歌浏览器及苹果手机的safari浏览器，两者都应该能正常播放；（已处理）
2、设置前端上传完成后，不继续等待后端的转换任务，前端的任务只是“完成上传”，转换操作由后端自己进行；（已完成）
web页面，卡片右上角菜单按钮面积增大（当前图标没问题，可以适当增加容器宽度），再调整删除按钮放大点（已处理）
当前转码基本改由电脑的python+ffmpeg去处理，特殊情况再由平板做临时处理（已处理）
替换转码文件unTransCode_ => Soonwin_（已处理）
调整AppBar按钮顺序 => 服务器页，添加视频功能，标签管理页，替换文件功能，全选功能（已处理）